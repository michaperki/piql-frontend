// CreateGame.test.js

test('Header component renders without errors', () => {
  // Your test code can be empty, or you can render the CreateGame component here.
  // For this example, we're keeping it simple.
});
